#include <stdio.h>

#include <string.h>

int main ()
{
    
    // char str1[100];
    // int i;
    // int str1_length=0;

    // printf("Enter the 1st  String \n");
    // gets(str1);

    // for ( i =0 ; str1[i] != '\0' ; i++);

    int i = 2;

    printf("The length is %d",!i);

    return 0;
    
}